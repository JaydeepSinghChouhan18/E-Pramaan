{
  "name": "e-pramaan",
  "version": "1.0.0",
  "private": true,
  "description": "e-Pramaan: National Trust & Verification Platform for Government Procurement",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace=shared",
    "build": "npm run build --workspace=shared && npm run build --workspace=backend && npm run build --workspace=frontend",
    "typecheck": "npm run build --workspace=shared && npm run typecheck --workspace=backend && npm run typecheck --workspace=frontend",
    "backend:dev": "npm run dev --workspace=backend",
    "backend:build": "npm run build --workspace=shared && npm run build --workspace=backend",
    "backend:start": "node backend/dist/index.js",
    "backend:typecheck": "npm run build --workspace=shared && npm run typecheck --workspace=backend",
    "frontend:dev": "npm run dev --workspace=frontend",
    "frontend:build": "npm run build --workspace=shared && npm run build --workspace=frontend",
    "frontend:typecheck": "npm run build --workspace=shared && npm run typecheck --workspace=frontend"
  }
}
